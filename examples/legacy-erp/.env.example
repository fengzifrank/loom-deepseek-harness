# DeepSeek API Key（对话需要；没有 key 也能 loom dev 起 health，只是不能对话）
DEEPSEEK_API_KEY=sk-你的key

# 老系统的 X-Api-Key（2018 年起就这一个 key；loom.app.ts 会把它映射给
# 导入生成物约定的 LOOM_IMPORT_TOKEN 注入位——见 loom.app.ts 路 1 注释）
LEGACY_API_KEY=legacy-key-2018
